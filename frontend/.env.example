# Copy to ".env" ONLY for split local development (React dev server on :3000
# talking to a separate backend on :8000).
#
# In production the frontend is served by the backend at the same origin, so API
# calls are relative and this variable is not needed.

REACT_APP_BACKEND_URL=http://localhost:8000
ENABLE_HEALTH_CHECK=false
